# IQL 파트 원고 (R&E 중간보고서 형식)
*"Data Augmentation for Data-Efficient Offline RL in Drone Flight Control" — 본론 中 IQL 부분*

> **형식 안내.** 최종본은 한글(HWP) 2018로 편집(A4·휴먼명조 10·줄간격 160%·표는 가로선만). 아래는 그 서식에 넣을 **내용 초안**이며 KSA 규정을 반영했다: 제목 번호 체계(가/나/다 → 1)/2)/3)), 표 제목은 **영문·표 위**(`<Table N>`), 그림 제목은 **영문·그림 아래**(`[Fig. N]`), 통계량은 *기울임*, 전문용어는 한글(원어) 병기, 참고문헌 인용은 `[n]`. 표 번호는 인용 순. 팀 보고서의 상위 번호(I·II, 1·2)는 공동 부분에 맞춰 재부여하면 된다.
>
> 값에 〔확인〕이 붙은 곳은 제출 전 원본 평가 출력(`EXPERIMENT_LOG.md`, `examples/*/`)과 대조해 확정할 것.

---

## [연구방법 中 IQL] — 다. IQL 기반 오프라인 RL과 데이터 증강

### 1) 과제 및 전문가 데이터
`gym-pybullet-drones` 시뮬레이터에서 쿼드로터(CF2X)가 닫힌 도형 경로(삼각형·사각형·오각형·원, 그리고 학습에 쓰지 않은 별)를 추종하는 과제를 다룬다. 전문가(expert) 정책은 순수추종(pure-pursuit)과 DSLPID 제어기로 매 스텝 목표 속도(target velocity) 명령을 생성한다. 제어는 속도 전용(velocity-only) 모드—위치 항을 0으로 두어 목표 위치를 현재 위치로 고정—로 두어, 기록된 행동이 상태 전이를 인과적으로 설명하도록 하였다(오프라인 RL의 Markov 가정 보전). 각 에피소드는 도형 크기·배치·시작 방위각(yaw)·평면 경사(±30°)를 무작위화하며 반시계/시계 양방향을 절반씩 수집한다.

### 2) 상태·행동 설계
상태는 16차원으로, 경로-상대 위치오차(3), 자세 사원수(quaternion, 4), 선속도(3), 각속도(3), 전방주시(look-ahead) 벡터(3)로 구성된다. 행동은 목표 속도 3차원이다. 위치오차만으로는 정책이 경로에 올라선 뒤(오차≈0) 진행 방향을 알 수 없어 정지·역주행한다. 전방주시 벡터(드론→경로상 전방 점)는 순수추종이 조향에 쓰는 경로-진행 신호를 상태에 명시하여 이를 해결한다. 상태가 절대좌표·도형별 특징 없이 경로-상대로만 구성되므로 미학습 도형·방향·경사로 일반화된다(결과 1) 참조).

### 3) IQL 학습과 DAgger
IQL[〈참고문헌 번호〉]은 상태 정규화, 행동 tanh 경계, 은닉 차원(hidden dim) 256, 기댓값 회귀 계수 *τ* = 0.85, 온도 *β* = 3.0으로 300k 스텝 학습한다. 롤아웃 시 목표 속도에 가속 제한(slew-rate limit)을 재적용하여 저수준 추종 발산을 막고, 보상 하한 절단(reward clipping, −1.0)으로 교란 데이터에서의 가치함수 발산을 안정화한다. DAgger[〈번호〉]는 현재 정책으로 드론을 주행시켜 정책이 실제 방문하는(이탈 포함) 상태를 수집하고, 그 상태에 순수추종의 정답 라벨을 재부여해 학습셋에 추가·재학습한다. 코너 복구 강화를 위해 수집 중 코너 부근 교란을 준다. 전체 절차는 **데이터 수집 → IQL 초기 학습 → DAgger×*N* → 재학습 → 평가**이다.

### 4) 생성 증강 (확산모델·GAN)
전이 *(s, a, s′, r)*를 36차원 벡터로 보고 확산모델(diffusion model)과 생성적 적대 신경망(GAN)으로 합성한다. 정보 누출을 막기 위해 각 설정이 사용하는 원본 부분집합만으로 생성기를 학습한다. 총 전이 수를 1.5M으로 고정한 채 실제:합성 비율을 바꾸어 동일 파이프라인으로 학습·비교한다.

### 5) 평가 프로토콜
완주 판정은 경로 진행도 기반 net-laps(최근접 경로 인덱스의 부호 있는 누적 변위 ÷ 경로 해상도)로 하며, 완주율은 정해진 시간창 내 net-laps ≥ 2.0인 롤아웃의 비율이다. 추적오차는 완주 롤아웃의 스텝별 위치오차 크기 평균(*M*)이다. 흔히 쓰는 최근접점까지 거리 평균은 정책이 한 곳에서 지그재그만 해도 작게 나와 경로 진행을 측정하지 못하므로 사용하지 않는다(방법론상 주의점). 평가는 held-out 시드(500–549) × 양방향 × 5도형(미학습 별 포함)으로 한다.

---

## [연구결과 및 논의 中 IQL] — IQL 결과

### 1) 실데이터 기본 성능과 일반화
충분한 실데이터로 학습한 IQL 정책은 4개 학습 도형과 미학습 별까지 양방향으로 완주한다([Fig. 1]). 단일 도형만 학습해도 나머지 미학습 도형으로 대부분 일반화되어(<Table 1>), 경로-상대 상태 설계의 강건성을 보인다—사각형만 학습해도 5도형 99.6% 완주, 원만 학습 시 86.8%. 이는 IQL이 상태 설계와 DAgger로 과제 자체를 해결함을 확인하는 기준선이다.

<Table 1> Completion rate and per-shape tracking error by training shape (original 0.1M, DAgger×1; unit: m)

| Train shape | Completion | circle | triangle | square | pentagon | star* |
|---|---|---|---|---|---|---|
| square | 99.6% | 0.059 | 0.146 | 0.153 | 0.141 | 0.180 |
| triangle | 98.8% | 0.035 | 0.133 | 0.153 | 0.149 | 0.172 |
| circle | 86.8% | 0.051 | 0.212 | 0.212 | 0.220 | 0.219 |
| mixed (4 shapes) | 94.8% | 0.086 | 0.174 | 0.186 | 0.181 | 0.198 |

*원본에 없는 도형(star). 원만 학습은 매끈해 코너 복구를 못 배워 완주율이 가장 낮다.

[Fig. 1] IQL policy tracking of five shapes (trained on real data). 목표 경로(점선)와 정책 비행 궤적(실선). 미학습 별을 포함해 모두 완주하며, 코너의 소폭 초과(overshoot)는 순수추종 전문가 자체의 한계를 모사한 것이다.

### 2) 원본 데이터량 축소 — 데이터 효율 기준선 (증강 없음)
증강 없이 실데이터를 1.5M→0.1M로 줄여도 완주 성능은 크게 무너지지 않으며, 특히 0.5M이 1.5M과 사실상 동등하다(완주율 97% 대 98%). 즉 IQL의 데이터 효율은 이미 높고, 완주를 결정하는 것은 원본 총량이 아니다(결과 5) 참조).

<Table 2> Performance vs. original data size (no augmentation) 〔확인: 완주율 정의〕

| Data size | Completion | *M* (m) |
|---|---|---|
| 1.5M | 98% | 0.384 |
| 1.0M | 94% | 0.658 |
| 0.5M | 97% | 0.347 |
| 0.1M | 80% | 0.743 |

### 3) 생성 증강과 IQL — DAgger에 조건부인 강건성
DAgger를 거친 최종 정책은 학습 소스가 실데이터든 순수 합성(GAN·확산모델)이든 전 도형 100% 완주를 달성하며(<Table 3>), 나아가 GAN 순수 합성으로 학습한 정책이 실데이터보다 정밀하였다(원 0.015 대 0.016 m, 코너 전반 더 낮음). 즉 IQL에서는 합성 데이터가 완주 측면에서 실데이터를 대체할 수 있고 정밀도는 오히려 개선될 수 있다.

<Table 3> Completion rate and precision by data source (real vs. pure GAN/diffusion, DAgger×2; error unit: m)

| Data source | Completion | circle | triangle | square | pentagon | star* |
|---|---|---|---|---|---|---|
| real data | 100% | 0.016 | 0.110 | 0.124 | 0.117 | 0.148 |
| GAN | 100% | 0.015 | 0.088 | 0.101 | 0.116 | 0.130 |
| diffusion | 100% | 0.023 | 0.107 | 0.126 | 0.118 | 0.150 |

*원본에 없는 도형(star).

다만 이 강건성은 DAgger에 조건부다. DAgger 없이 초기 정책만으로 합성 혼합을 학습하면 합성 비율이 높을수록·합성이 깨끗할수록 폐루프에서 발산한다(완주율 급락). 깨끗한 on-path 합성에는 이탈→복구 표본이 없어(coverage hole) 정책이 복구를 못 배우고, DAgger가 바로 그 복구 커버리지를 메워 완주를 평준화하기 때문이다. 따라서 데이터의 청결도(cleanliness)와 DAgger 이전 강건성은 상반된다(실제 < 확산 < GAN 순으로 깨끗해질수록 DAgger 전엔 더 불안정, 후엔 더 정밀). 〔확인: 초기-학습 혼합 발산 수치 인용〕

### 4) 합성 데이터의 물리 일관성 (동역학 잔차)
합성 전이의 물리 일관성을 *s*→*s′* 동역학 잔차로 측정하면 실데이터가 가장 정확하고(0.015 m) GAN이 확산모델보다 잔차가 낮다(0.038 대 0.096 m). 이는 결과 3)에서 GAN 증강이 더 안정·정밀했던 것과 일치한다.

<Table 4> Physical consistency of synthetic data (dynamics residual)

| Source | Dynamics residual (m) | Off-path >0.2m | Off-path >0.5m |
|---|---|---|---|
| real data | 0.015 | 0.2% | 0.2% |
| GAN | 0.038 | 3.1% | 1.4% |
| diffusion | 0.096 | 2.9% | 2.6% |

### 5) DAgger가 완주율과 정밀도를 결정한다 (핵심 결과)
IQL 파트의 중심 발견은 완주율과 정밀도의 결정자가 원본 데이터량이나 소스가 아니라 DAgger라는 것이다.

**가) DAgger 없는 IQL의 한계.** DAgger 없이 도달 가능한 완주율을 6개 모델의 행동 평균 앙상블로 측정하였다. 정해진 시간창에서는 완주율이 약 49.6%에서 천장에 부딪히나 이는 속도 인공물이다—미완주 롤아웃은 경로를 이탈하는 것이 아니라 시간-최적 대비 약 55% 속도로 느리게 도는 것이었다(진단으로 확인). 시간을 충분히 주면 완주율이 96–100%로 수렴한다(<Table 5>). 즉 경로 추종 능력 자체는 DAgger 없이도 거의 완성되나 속도·정밀도에 하자가 남는다.

<Table 5> Maximum completion without DAgger (IQL init 300k, six-model action ensemble, endurance condition)

| Eval shape | Completion | *M* (m) |
|---|---|---|
| triangle | 96% | 0.451 |
| square | 98% | 0.443 |
| pentagon | 99% | 0.404 |
| circle | 100% | 0.236 |
| star* | 100% | 0.471 |

*미학습 도형. 단, 다른 모델 대비 평균 랩 속도가 약 1.8배 느림.

**나) DAgger 한 번이 두 하자를 동시에 해소.** 위 앙상블이 실제 방문하는(배포) 상태 분포에서 DAgger를 1회 수집해 원본에 병합하고 단일 모델을 재학습하면, 고정시간 완주율이 24%→100%로 오르고 순항 오차가 코너에서 약 4배(0.6→0.13 m)·원에서 약 19배(0.69→0.036 m) 줄며, 최악 스텝 오차가 3–4 m→≤0.66 m로 꼬리가 사라지고 속도도 빨라진다. 앙상블의 49.6% 천장은 순전히 'DAgger 없음' 때문이었고, DAgger 1회 후에는 단일 모델이 앙상블을 모든 지표에서 능가한다.

<Table 6> Effect of a single DAgger pass (one model, 10 seeds × both directions × 5 shapes, fixed-time)

| Metric | No DAgger | DAgger×1 |
|---|---|---|
| Completion @2.0 lap | 24/100 (24%) | 100/100 (100%) |
| Cruise error, corners (*M*) | 0.45–0.62 m | 0.12–0.16 m |
| Cruise error, circle (*M*) | 0.69 m | 0.036 m |
| Worst-step max | 3.0–4.0 m | ≤0.66 m |

**다) 종합.** 전방주시 상태가 과제를 학습 가능하게 만들고 DAgger가 그것을 완성(완주율+정밀도)시킨다. 데이터량을 15배 줄여도(0.1M) DAgger가 있으면 완주·정밀도가 유지되며, 데이터를 늘리거나 소스를 바꾸는 것은 이 결정력을 대체하지 못한다. 증강(GAN·확산)은 DAgger가 커버리지를 메워주는 한 실데이터를 대체할 수 있는 보조 수단이다.

### 6) 소결
첫째, 경로-상대(전방주시) 상태로 IQL은 단일 정책으로 임의 도형을 추종하고 미학습 도형·방향·경사로 일반화된다. 둘째, 완주율·정밀도의 결정자는 원본 데이터량이나 소스가 아니라 DAgger로, DAgger 없는 천장(고정 49.6% / 지구력 96–100%)이 DAgger 1회로 단일 모델 100%·고정밀로 해소된다. 셋째, 그러므로 IQL은 데이터 효율이 높고 증강에 강건하여, 합성(특히 GAN)으로 실데이터를 완주 측면에서 대체할 수 있고 DAgger가 있으면 정밀도까지 유지·개선된다.

---

### 제출 전 처리 목록
1. 표 번호를 팀 보고서 전체 순서에 맞춰 재부여(현재 IQL 내부 순서 <Table 1–6>).
2. 〔확인〕 표시 수치(완주율 정의, 초기-학습 혼합 발산 값)를 원본 eval 출력과 대조.
3. IQL·DAgger·SynthER 등 참고문헌 번호 채우기(`[n]`), 본문 인용 위치 표시.
4. HWP에서 표를 가로선만으로 재작성, 통계 기호(*M*, *τ*, *β*, *s*, *s′*) 기울임 확인.
5. TD3+BC·CQL 결과와의 대비 1문단(같은 증강이 알고리즘마다 다른 이유: 부트스트랩 발산 대 IQL의 기댓값 회귀 안정성)—공동 저자와 조율.
