/.DS_Store
.DS_Store

# --- large / regenerable artifacts (rebuild via README pipeline) ---
*.csv
# per-episode shape_dataset archives are local-only (regenerable; the merged .csv.gz is enough)
*.tar.gz
*.zip
*.npz
*.log
*.txt
policy_paths.png

IQL-PyTorch-main/runs/
dataset_1M/
**/shape_dataset/
**/__pycache__/
*.pyc
*.egg-info/
.idea/
.vscode/

# CQL probe (d3rlpy) — local-only, not part of the offline-RL/IQL project deliverables
drone_stacked_training_code/
**/_cql_*.py

